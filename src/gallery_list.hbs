{{#each this}}
<a class="photo-card">
    <img src="{{this.result.backdrop_path}}" alt="{{this.result.imdb_id}}" loading="lazy" />
    <div class="info">
        <p class="info-item">
            <b>Name {{this.result.result.original_title}}</b>
        </p>
        <p class="info-item">
            <b>Budget {{this.result.budget}}</b>
        </p>
        <p class="info-item">
            <b>Comments {{this.result.tagline}}</b>
        </p>
        <p class="info-item">
            <b>Vote Count {{this.vote_count}} </b>
        </p>
    </div>
</a>
{{/each}}